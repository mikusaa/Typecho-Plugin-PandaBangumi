# Change log | 更新日志

## 未发布

* 新增：[API 设置] 支持官方 Bangumi API 和自定义 HTTPS API 镜像，镜像地址仅在服务端使用，不再暴露为页面全局变量。
* 新增：[封面设置] 增加直连与本站缓存两种封面加载模式，日历、收藏列表和 Subject 详情卡均可使用本站封面缓存。
* 新增：[收藏分类] 增加动画、书籍、游戏和三次元四种收藏分类，并支持各分类的在看和已看列表。
* 新增：[音乐收藏] 增加音乐分类的在听和听过列表，使用方形封面并以曲目显示收听进度。
* 新增：[收藏分页] 收藏列表支持分批加载、失败重试和跳转 Bangumi 查看更多；首批最多显示 11 条并保留一张操作卡，后续每批最多加载 12 条。
* 新增：[日历筛选] 日历支持“仅在看”和“全部日历”过滤，移动端增加星期折叠控件和当天空状态。
* 新增：[Subject] 为书籍、游戏和三次元条目适配对应的元信息展示。
* 新增：[封面加载] 增加共享懒加载控制器、加载占位动画、失败占位和减少动态效果支持。
* 新增：[品牌标识] 浏览器控制台增加一次性的 PandaBangumi 品牌标识。
* 新增：[测试] 增加零依赖 PHP 测试框架和固定 fixture，不访问真实 Bangumi API，也不写入插件运行缓存。
* 新增：[测试] 增加 24 项核心回归测试，覆盖配置、请求参数、分页、缓存生命周期、缓存防护、限流、并发槽、90 天清理、配额、响应上限、封面版本绑定、SSRF 基础规则、全路由 429、收藏刷新、日历过滤和 Subject 刷新。
* 新增：[测试] 增加零依赖 Node.js fixture 测试，覆盖单部 Subject 卡片的类型映射、字段降级和数据规范化。
* 优化：[API 设置] API 镜像与图片来源完全解耦，不再从 `ApiBase` 推导图片地址。
* 优化：[API 请求] 加固请求超时、TLS 校验、响应状态检查和错误处理，JSON 响应在接收过程中限制为 4 MiB。
* 优化：[API 请求] Bangumi User-Agent 自动使用当前插件版本并包含项目地址。
* 优化：[封面来源] 封面统一使用 API 返回的 `images.large` 字段，不修改 URL 猜测尺寸，也不回退到其他尺寸。
* 优化：[封面安全] 仅接受公网 HTTPS 443 图片地址，拒绝重定向，验证全部 DNS 解析结果并固定解析地址；下载限制为 5 MiB，并校验真实 MIME 类型。
* 优化：[封面接口] 使用源 URL 和尺寸生成 16 位 `cover_version`，并回查缓存 JSON 验证，避免封面接口成为任意图片代理。
* 优化：[封面缓存] 增加 `ETag`、`Last-Modified`、一年 `Cache-Control`、`Expires` 和条件请求 `304`。
* 优化：[封面清理] 恢复未引用封面 90 天保留，每日最多维护一次；封面继续限制为最多 2048 个文件和 512 MiB，成功写入后立即按修改时间淘汰未受保护的旧文件。
* 优化：[缓存目录] 缓存布局升级为 v2 并迁移到隐藏的 `.cache/`；数据使用 PHP `404` 前缀，各子目录保留入口守卫和 Apache 拒绝规则。
* 优化：[数据缓存] 增加 64 槽固定分片锁、非阻塞刷新、锁后二次检查、同目录原子写入、陈旧数据回退和 5 分钟失败退避；Subject 缓存最多保留 256 项。
* 优化：[接口限流] Subject、日历和收藏分页统一使用容量 20 的 API 令牌桶，封面使用容量 32 的令牌桶；API 与封面再共享最多 2 个真实外部请求并发槽，全部路由均支持 `429` 与 `Retry-After`。
* 优化：[缓存防护] `.cache` 自动补齐 PHP `404` 入口守卫和 Apache 拒绝规则；初始化失败时停止运行，不探测、迁移或删除旧 `cache/`。
* 优化：[收藏缓存] 合法空收藏作为成功结果缓存；只有确认上游分页完整后才覆盖旧数据，并严格按配置的 `Limit` 截断。
* 优化：[Subject] Subject 数据改由本站接口获取，避免浏览器直接访问 API 镜像。
* 优化：[Subject 卡片] 重构为主题无关的紧凑横向组件，正式适配动画、书籍、游戏、三次元和音乐，未知类型保留通用降级展示。
* 优化：[Subject 卡片] 桌面端增加两行简介、精简评分和类型元信息；移动端保持紧凑横排并减少次要信息。
* 优化：[音乐卡片] 单部音乐 Subject 改为唱片资料标签布局，使用方形 CD 盒封面、实体侧标、独立创作者行及曲目、碟片、收藏规格栏。
* 优化：[音乐取色] 本站缓存封面加载后自动提取代表色并用于侧标，过滤黑白灰并保证白字对比度；直连封面或取色失败时使用默认色。
* 优化：[卡片布局] 收藏列表和日历统一使用 `2:3` 封面卡片、遮罩和响应式网格，收藏列表按状态展示进度。
* 优化：[懒加载] 严格按视口加载封面；日历只观察当前星期，切换星期后再加载新面板图片。
* 优化：[前端生命周期] 统一由 `window.PandaBangumi.init()` 管理，并监听常见 PJAX、Turbo 和 Turbolinks 事件。
* 优化：[前端生命周期] 页面切换前取消请求、断开 IntersectionObserver，并取消未完成的封面预加载。
* 优化：[前端并发] 本站 JSON 与缓存封面统一进入 2 槽同源请求队列，PJAX 离开页面时取消活动任务并清空等待任务。
* 优化：[运行环境] 最低 PHP 版本调整为 8.0，插件启用时检查 curl 和 openssl 扩展。
* 调整：[代码结构] 将内部 PHP 实现迁移到 `src/`，按配置、HTTP、缓存、封面及收藏、日历、Subject 服务拆分职责。
* 调整：[代码结构] 根目录 `BangumiAPI.php` 改为兼容门面，保留 `Action.php` 使用的公开静态接口，文件由 1423 行缩减为 184 行。
* 调整：[项目链接] 插件介绍页改为当前维护版本的独立说明页面，避免继续指向差异较大的原版文章。
* 修复：[日历封面] 修复 Bangumi 日历返回 `http://lain.bgm.tv` 图片时 HTTPS 页面无法显示封面的问题，仅在服务器缓存下载时将官方地址升级为 HTTPS。
* 修复：[API 设置] 修复镜像地址携带路径、用户信息、查询参数或非法协议时的规范化问题。
* 修复：[前端安全] 加固 URL 校验和 DOM 输出，避免直接插入上游 HTML。
* 修复：[前端生命周期] 修复 PJAX 重复初始化、重复资源注入和旧请求继续更新新页面的问题。
* 修复：[收藏缓存] 修复刷新失败时列表突然清空、并发重复刷新、失败请求风暴和合法空收藏未缓存的问题。
* 修复：[封面隐私] 修复缓存模式可能泄露 API 镜像或图片源地址的问题，下载失败时不再跳转外部图片。
* 修复：[封面缓存] 覆盖 Typecho 登录会话预先写入的 `no-cache/no-store` 响应头，避免登录用户重复下载封面。
* 修复：[条目信息] 修复书籍、游戏进度和评分展示中的类型差异，游戏不再伪造动画式进度。
* 修复：[收藏状态] 书籍改用 `reading/read`、游戏改用 `playing/played`、音乐改用 `listening/listened`，动画和三次元继续使用 `watching/watched`。
* 修复：[追番日历] 日历卡片不再误用收藏列表的观看进度，移除无意义的 `0 / 未知` 和底部进度条。
* 移除：[旧版配置] 删除 `ProxyImages` 和 `CacheCollectionImages` 配置兼容，旧版本跨版本升级需先卸载旧插件，再安装并重新配置。
* 移除：[缓存迁移] 不再读取、迁移、检查或自动删除旧缓存；旧版用户必须删除整个旧插件目录后全新安装，直接覆盖升级不受支持。
* 移除：[前端兼容] 删除旧全局入口 `window.initCollection()`，主题自定义回调应改用 `window.PandaBangumi.init()`。
* 移除：[前端兼容] 删除收藏占位节点后方旧 `.loader` 元素的清理逻辑。
* 移除：[Subject 卡片] 删除对 VOID 正文容器 `.yue`、旧内部卡片类和全局图标类的依赖，改用组件命名空间及可覆盖 CSS 变量。
* 移除：[API 设置] 页面不再注入 `window.bgmApiBase`，浏览器端只保留本站接口地址 `window.bgmBase`。

注意，以上内容当前使用 `3.0.6` 开发版本验证，尚未正式发布或打标签。

**🍰 2026-03-07 Version 3.0.2**

* 优化：[日历] 标签页居中显示，移动端打开日历时自动滚动到当天。
