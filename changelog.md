# 更新日志

## 4.0.0 - 2026-08-05

PandaBangumi 4.0 对收藏展示、追番日历、条目卡片和数据加载方式进行了全面重构。

### 重要变更

- 最低运行环境调整为 PHP 8.0，并要求启用 curl 和 openssl。
- 不支持从旧版本直接覆盖升级；请停用插件、删除旧目录后重新安装并配置。
- 不再读取或迁移旧版缓存，也不再兼容已移除的旧图片配置，并移除了 `window.initCollection()` 等旧前端入口。
- 项目采用 MIT 许可证发布，保留 AlanDecode、Izumiko 与 mikusaa 的版权归属。

### 新增

- 支持动画、书籍、游戏、三次元和音乐收藏，并使用各分类对应的在看与已看状态。
- 收藏列表支持按需分页和前往 Bangumi 查看更多；追番日历支持“仅在看”筛选及移动端星期控件。
- 条目卡片适配动画、书籍、游戏、三次元和音乐，并为音乐提供独立的唱片信息布局。
- 设置页新增代码速查与一键复制；文章编辑器新增条目卡片按钮，支持输入 Subject ID 或粘贴 Bangumi 条目链接。
- 支持自定义 HTTPS Bangumi API 镜像，以及封面直连和本站缓存两种加载方式。

### 改进

- 重新设计收藏列表、日历和条目卡片的响应式布局、懒加载、加载骨架、错误状态和减少动态效果支持。
- Subject、日历和收藏数据统一通过本站接口加载，不再向浏览器暴露 API 镜像；本站缓存封面支持长期缓存和条件请求。
- 重构数据缓存与刷新流程，遇到限流或短暂故障时优先继续使用已有缓存，并限制重复刷新和真实上游并发。
- 加固请求参数、上游 URL、响应大小和缓存目录，降低异常输入、恶意请求及公开缓存带来的风险。
- 统一前端初始化与请求生命周期，改善常见 PJAX、Turbo 和 Turbolinks 场景中的重复加载及页面切换问题。

### 修复

- 修复追番日历筛选后内容为空、误用收藏进度以及初始化时带动页面纵向滚动的问题。
- 修复各收藏分类的状态、进度和评分展示差异，并使日历的在看集合不再受收藏列表数量上限影响。
- 修复缓存模式下封面来源泄露、登录用户重复下载、冷启动瞬时失败和旧数据刷新失败后列表清空的问题。

## 3.0.2 - 2026-03-07

- 调整追番日历标签布局，并在移动端自动定位到当天。

## 3.0.1 - 2026-03-07

- 加固缓存读取、日历响应和前端文本输出，并改用固定资源文件名配合版本参数刷新缓存。

## 3.0.0 - 2026-03-07

- 数据源切换到 Bangumi API，移除旧网页解析和内置 jQuery。
- 新增收藏列表、追番日历和单部条目卡片，并重做响应式样式与夜间模式适配。
- 重构后端接口和缓存读取，已看收藏支持跨页获取。
